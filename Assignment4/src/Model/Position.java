/*
    Defines opponent positions as an
    enum: LEFT, MIDDLE, RIGHT.
    Used for targeting attacks and organizing opponent team layout.
*/
package Model;

public class Position {
    public enum PositionType {
        LEFT,
        MIDDLE,
        RIGHT
    }
}
